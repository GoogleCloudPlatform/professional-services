# rsync Simple Upload Example

This is an example of a simple file upload to a server.

## Prerequisite

The `docker-compose` builder is required to run this example.

## Building

Using `gcloud` tool:

```
gcloud builds submit --config cloudbuild.yaml .
```
