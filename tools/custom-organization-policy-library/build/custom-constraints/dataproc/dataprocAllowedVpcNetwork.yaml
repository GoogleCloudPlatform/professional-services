#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("dataprocAllowedVpcNetwork")

#@ def condition(vpcNetworkUri):
#@    return "(resource.config.gceClusterConfig.networkUri in " + str(vpcNetworkUri) + ") == false"
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types: 
- dataproc.googleapis.com/Cluster 
#! this is not working yet, exclusive network/subnetwork config issue, shared vpc permutation and network/subnetwork string not matching.
condition: #@ condition(constraint.params().vpcNetworkUri)
action_type: DENY
method_types: 
- CREATE
- UPDATE 
display_name: Allow only authorized VPC network for Dataproc
description:  Enforce that the Dataproc cluster is created with in an authorized VPC network
#@ end
