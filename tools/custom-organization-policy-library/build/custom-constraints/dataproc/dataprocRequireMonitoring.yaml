#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("dataprocRequireMonitoring")

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types: 
- dataproc.googleapis.com/Cluster 
condition: 1 == 0
action_type: DENY
method_types: 
- CREATE
- UPDATE 
display_name: Enable Cloud Monitoring Agent for Dataproc
description:  Enforce that the Dataproc cluster is configured with Cloud Monitoring Agent enabled
#@ end
