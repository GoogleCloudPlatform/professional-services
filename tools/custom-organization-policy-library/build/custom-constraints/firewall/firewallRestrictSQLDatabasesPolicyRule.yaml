#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("firewallRestrictSQLDatabasesPolicyRule")

#@ def condition():
#@   #! SQL Relational Databases
#@   #! MySQL: TCP 3306
#@   #! Oracle: TCP 1521, 2483-2484; UDP 2483-2484
#@   #! PostgreSQL: TCP/UDP 5432
#@   #! Deny if source range is 0.0.0.0/0 (any source)
#@   #! Split port checks to keep lines under 120 characters
#@   tcp_ports = "port == '3306' || port == '1521' || port == '2483'"
#@   tcp_ports2 = "port == '2484' || port == '5432'"
#@   udp_ports = "port == '2483' || port == '2484' || port == '5432'"
#@
#@   lines = [
#@     "resource.rules.exists(rule,",
#@     "  rule.priority < 2147483644 &&",
#@     "  rule.direction == 'INGRESS' &&",
#@     "  rule.match.srcIpRanges.exists(range, range == '0.0.0.0/0') &&",
#@     "  rule.match.layer4Configs.exists(l4config,",
#@     "    l4config.ipProtocol == 'tcp' &&",
#@     "    l4config.ports.exists(port,",
#@     "      " + tcp_ports + " || " + tcp_ports2,
#@     "    ) ||",
#@     "    l4config.ipProtocol == 'udp' &&",
#@     "    l4config.ports.exists(port, " + udp_ports + ")",
#@     "  )",
#@     ")"
#@   ]
#@   return "\n".join(lines)
#@ end

#@ def description():
#@   lines = [
#@     "Ensure that SQL database ports (MySQL, Oracle, PostgreSQL) are not accessible from any source when",
#@     "using Firewall Policy Rule",
#@   ]
#@   return "\n".join(lines)
#@ end

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types:
- compute.googleapis.com/FirewallPolicy
condition: #@ condition()
action_type: DENY
method_types:
- CREATE
- UPDATE
display_name: Restrict Firewall Policy rules allowing SQL database port access from any source
description: #@ description()
#@ end
