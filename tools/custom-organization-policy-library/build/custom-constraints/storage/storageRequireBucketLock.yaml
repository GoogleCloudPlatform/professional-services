#@ load("/constraints.lib.star", "build_constraint")
#@ constraint = build_constraint("storageRequireBucketLock")

#@ if constraint.to_generate():
name: #@ constraint.constraint_name()
resource_types: 
- storage.googleapis.com/Bucket 
condition: 1 == 0
#! condition: has(resource.retentionPolicy.isLocked) # This is not working, no option to lock on create using console/gcloud.
action_type: ALLOW
method_types: 
- CREATE
- UPDATE 
display_name: Require bucket lock 
description:  Enforce Cloud Storage bucket lock to be configured
#@ end
