name: organizations/11111111/customConstraints/custom.firewallRestrictNetworkServicesPolicyRule
resource_types:
  - compute.googleapis.com/FirewallPolicy
condition: |-
  resource.rules.exists(rule,
    rule.priority < 2147483644 &&
    rule.direction == 'INGRESS' &&
    rule.match.srcIpRanges.exists(range, range == '0.0.0.0/0') &&
    rule.match.layer4Configs.exists(l4config,
      (l4config.ipProtocol == 'tcp' && l4config.ports.exists(port, port == '53' || port == '137' || port == '138' || port == '139')) ||
      (l4config.ipProtocol == 'udp' && l4config.ports.exists(port, port == '53' || port == '137' || port == '138' || port == '139'))
    )
  )
action_type: DENY
method_types:
  - CREATE
  - UPDATE
display_name: Restrict Firewall Policy rules allowing network service access from any source
description: |-
  Ensure that network infrastructure services (DNS, NetBIOS) are not accessible from any source when
  using Firewall Policy Rule
