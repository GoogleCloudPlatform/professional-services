name: organizations/11111111/customConstraints/custom.firewallRestrictCacheSearchDatabasesRule
resource_types:
  - compute.googleapis.com/Firewall
condition: |-
  resource.direction.matches('INGRESS') &&
    resource.sourceRanges.exists(range, range == '0.0.0.0/0') &&
    resource.allowed.exists(rule,
      rule.ports.exists(port,
          (rule.IPProtocol == 'tcp' && port in ['9200', '9300', '11211', '11214', '11215', '6379']) ||
          (rule.IPProtocol == 'udp' && port in ['11211', '11214', '11215'])
      )
    )
action_type: DENY
method_types:
  - CREATE
  - UPDATE
display_name: Restrict VPC Firewall rules allowing cache/search database port access from any source
description: |-
  Ensure that cache and search database ports (Elasticsearch, Memcached, Redis) are not accessible
  from any source when using VPC Firewall Rule.
