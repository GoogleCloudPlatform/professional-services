custom.dataprocAllowedImages:
  action_type: DENY
  condition: 1 == 0
  description: Enforce that Dataproc cluster is created with authorized image versions
  display_name: Allow only authorized images for Dataproc
  method_types:
  - CREATE
  - UPDATE
  resource_types:
  - dataproc.googleapis.com/Cluster
