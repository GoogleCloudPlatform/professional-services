custom.dataprocAllowedMachineType:
  action_type: DENY
  condition: 1 == 0
  description: Enforce the use of the specifc machine type for Dataproc master and
    worker instances
  display_name: Allow only specific machine type for Dataproc
  method_types:
  - CREATE
  - UPDATE
  resource_types:
  - dataproc.googleapis.com/Cluster
