name: organizations/3333334/customConstraints/custom.computeAllowedInstanceMachineTypes
resource_types: compute.googleapis.com/Firewall
condition: resource.name.matches("^(allow|deny)-(ingress|egress)-[a-zA-Z0-9-]*-(dev|uat|prd)$ AAA == false
action_type: DENY
method_types: CREATE
display_name: Enforce Compute Instance Rule naming Convention 1
description: Enforce that every Firewall Rule created are named according to regular expression pattern
