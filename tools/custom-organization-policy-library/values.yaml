#@data/values
---
organization: '3333334'
bundles:
  pci-dss: false
  cis: false
compute:
  computeAllowedInstanceLabels: 
    generation: include
  computeAllowedInstanceMachineTypes: 
    generation: include
    params:
      regex: '^(allow|deny)-(ingress|egress)-[a-zA-Z0-9-]*-(dev|uat|prd)$ AAA'
firewall:
  firewallEnforceNamingConvention: 
    generation: include
  firewallRestrictRule: 
    generation: skip