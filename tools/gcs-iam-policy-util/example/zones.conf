# This configuration file defines security zones of buckets and prefixes
#
# Format:
# [alias]
# bucketName (prefix)
#
# If a prefix is not provided, policies will apply to the entire bucket
#
[opendata]
bucket1
bucket2 prefix2/subdir

[restricted]
bucket3
bucket4 prefix4/subdir
