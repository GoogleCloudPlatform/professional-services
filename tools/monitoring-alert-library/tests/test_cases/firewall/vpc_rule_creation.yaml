shared_config:
  default_markers:
    - firewall
    - firewall-vpc-rule

firewall_vpc_creation_audit_log:
  steps:
  - log_filter: >-
        resource.type="gce_firewall_rule" AND (protoPayload.methodName:"compute.firewalls.patch" OR protoPayload.methodName:"compute.firewalls.insert" OR protoPayload.methodName:"compute.firewalls.delete")
    command: gcloud compute firewall-rules create {{ identifier }}
    command_flags: 
      action: ALLOW
      direction: INGRESS
      network: monitoring-alert-vpc-network 
      priority: 1000  
      rules: tcp:3389 
      source-ranges: 192.168.1.0/24
    teardown_command: gcloud compute firewall-rules delete {{ identifier }}
    expected_result:
      return_code: 0
      attributes:
        'protoPayload.resourceName': "projects/dbs-validator-kcc-29ae/global/firewalls/monitoring-alert-library-firewall-vpc-creation"
        'protoPayload.resourceOriginalState.direction': INGRESS



# gcloud logging read 'resource.type="gce_firewall_rule" AND (protoPayload.methodName:"compute.firewalls.patch" OR protoPayload.methodName:"compute.firewalls.insert" OR protoPayload.methodName:"compute.firewalls.delete")' --project dbs-validator-kcc-29ae  --format json --limit 1 --order=desc --freshness=10m
# gcloud alpha logging tail 'resource.type="gce_firewall_rule" AND (protoPayload.methodName:"compute.firewalls.patch" OR protoPayload.methodName:"compute.firewalls.insert" OR protoPayload.methodName:"compute.firewalls.delete")' --project dbs-validator-kcc-29ae  --format json
