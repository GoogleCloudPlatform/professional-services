name: organizations/[ORGANIZATIONID]/customConstraints/custom.identitydefaults
resourceTypes:
- container.googleapis.com/NodePool
methodTypes:
- CREATE
- UPDATE
condition: resource.config.serviceAccount == "default"
actionType: DENY
displayName: identitydefaults
description: Custom Constraint to require a specific service account to be assigned to the GKE Node